model:
  base_model: sd15

data:
  dataset_path: ./examples/fern/dataset_512
  resolution: 512
  cache_latents: false

lora:
  rank: 8
  alpha: 8

training:
  # Increased LR for faster adaptation; change to 5e-5 if unstable
  learning_rate: 2e-5
  batch_size: 1
  # Run longer as requested
  max_train_steps: 1000
  preset: balanced
  mixed_precision: fp32
  enable_xformers: false
  seed: 42
  # Produce intermediate samples every N steps so you can inspect progress
  sample_every_n_steps: 100
  sample_num_images: 4

output:
  output_dir: ./runs/test_fern_1000
